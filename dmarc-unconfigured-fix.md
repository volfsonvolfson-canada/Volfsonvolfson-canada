# 🔧 Исправление: DMARC запись не активируется

## ❌ Проблема

**DMARC запись (`_dmarc.new.backtobase.ca`) имеет статус "Unconfigured"**

**Остальные записи активированы, но DMARC нет.**

---

## 🔍 Причины и решения

### Причина 1: DNS запись еще не распространилась

**DMARC запись может распространяться дольше, чем другие записи.**

**Решение:**
1. Подождите еще 10-30 минут
2. Проверьте через MXToolbox: https://mxtoolbox.com/TXTLookup.aspx
   - Введите: `_dmarc.new.backtobase.ca`
   - Должна показаться DMARC запись
3. Если запись видна в MXToolbox - вернитесь в Mailgun и нажмите "Activate" или обновите страницу

---

### Причина 2: Неправильное имя записи

**В DNS записи имя должно быть:**
- `_dmarc` (без домена!)

**Проверьте в Zone Editor:**
- Имя записи должно быть: `_dmarc`
- НЕ: `_dmarc.new.backtobase.ca` (система сама добавит домен)

---

### Причина 3: Неправильное значение записи

**Значение DMARC должно быть:**
```
v=DMARC1; p=none; pct=100; fo=1; ri=3600; rua=mailto:ca560f54@dmarc.mailgun.org,mailto:6025edc5@inbox.ondmarc.com; ruf=mailto:ca560f54@dmarc.mailgun.org,mailto:6025edc5@inbox.ondmarc.com;
```

**Проверьте:**
- Нет ли лишних пробелов
- Все ли символы скопированы правильно
- Нет ли разрывов строк

---

### Причина 4: Mailgun еще не проверил запись

**Mailgun проверяет DNS записи периодически, не сразу.**

**Решение:**
1. Нажмите кнопку "Activate" (если есть) или "Verify" (если есть)
2. Или обновите страницу в Mailgun
3. Mailgun должен автоматически проверить запись

---

## 🔍 Пошаговая проверка

### Шаг 1: Проверить DNS запись через MXToolbox

1. Откройте: https://mxtoolbox.com/TXTLookup.aspx
2. Введите: `_dmarc.new.backtobase.ca`
3. Нажмите "TXT Lookup"

**Что должно быть:**
- ✅ Должна показаться DMARC запись с правильным значением
- ❌ Если запись не найдена - DNS запись еще не распространилась или неправильно добавлена

---

### Шаг 2: Проверить запись в Zone Editor

**В Zone Editor проверьте:**

1. **Имя записи:**
   - Должно быть: `_dmarc` (без домена!)
   - НЕ: `_dmarc.new.backtobase.ca`

2. **Тип записи:**
   - Должен быть: `TXT`

3. **Значение (TXT Data):**
   - Должно быть: `v=DMARC1; p=none; pct=100; fo=1; ri=3600; rua=mailto:ca560f54@dmarc.mailgun.org,mailto:6025edc5@inbox.ondmarc.com; ruf=mailto:ca560f54@dmarc.mailgun.org,mailto:6025edc5@inbox.ondmarc.com;`
   - Без пробелов после запятых в `mailto:...`
   - Все в одной строке

---

### Шаг 3: Если запись неправильная

**Если в Zone Editor запись неправильная:**

1. **Нажмите "Edit"** рядом с записью `_dmarc`
2. **Исправьте:**
   - Name: `_dmarc` (без домена!)
   - Type: `TXT`
   - TTL: `3600`
   - TXT Data: `v=DMARC1; p=none; pct=100; fo=1; ri=3600; rua=mailto:ca560f54@dmarc.mailgun.org,mailto:6025edc5@inbox.ondmarc.com; ruf=mailto:ca560f54@dmarc.mailgun.org,mailto:6025edc5@inbox.ondmarc.com;`
3. **Сохраните**

---

### Шаг 4: Подождать и проверить

**После исправления:**

1. Подождите 5-30 минут для распространения DNS
2. Проверьте через MXToolbox
3. Вернитесь в Mailgun и нажмите "Activate" или обновите страницу

---

## ⚠️ Важные моменты

1. **Имя записи:**
   - Правильно: `_dmarc`
   - Неправильно: `_dmarc.new.backtobase.ca` (система сама добавит домен)

2. **Значение записи:**
   - Должно быть в одной строке
   - Без пробелов после запятых в списке email адресов
   - Все символы должны быть скопированы правильно

3. **Распространение DNS:**
   - DMARC запись может распространяться дольше других
   - Подождите и проверьте через MXToolbox

---

## 🎯 Что делать сейчас

1. **Проверьте запись в Zone Editor:**
   - Правильное ли имя (`_dmarc`)?
   - Правильное ли значение?

2. **Проверьте через MXToolbox:**
   - Видна ли запись `_dmarc.new.backtobase.ca`?

3. **Если запись видна в MXToolbox:**
   - Вернитесь в Mailgun
   - Нажмите "Activate" или обновите страницу
   - Mailgun должен автоматически проверить запись

4. **Если запись не видна в MXToolbox:**
   - Проверьте, правильно ли добавлена запись в Zone Editor
   - Подождите еще немного для распространения DNS

---

## 📞 Сообщите результат

**Проверьте и сообщите:**
1. Какое имя у DMARC записи в Zone Editor?
2. Видна ли запись через MXToolbox?
3. Есть ли кнопка "Activate" в Mailgun?

**Сообщите результаты проверки!** 🚀



