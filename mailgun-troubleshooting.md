# 🔍 Диагностика: Почему Mailgun показывает 0 отправленных писем

## ❓ Возможные причины

### 1. Бронирование не создается через API

**Проблема:** Если вы просто добавили заказ в `order.html` (через localStorage), то API `create_booking` НЕ вызывается, и письма НЕ отправляются.

**Решение:** 
- Создайте бронирование через форму на странице комнаты (например, `room-basement.html`)
- Заполните форму и нажмите "Book"
- Только тогда будет вызван API `create_booking` и отправлены письма

---

### 2. Email не в списке авторизованных получателей

**Проблема:** Sandbox Domain может отправлять письма ТОЛЬКО на email-адреса из списка авторизованных.

**Решение:**
1. Mailgun Dashboard → **Sending** → **Domain Settings**
2. Выберите Sandbox Domain
3. Вкладка **Authorized Recipients**
4. Убедитесь, что email гостя и хозяина добавлены и подтверждены

---

### 3. Ошибки при отправке (но не видно)

**Проблема:** Ошибки могут логироваться, но не отображаться пользователю.

**Решение:** Проверьте логи на хостинге (PHP error logs)

---

### 4. Неправильный API ключ или Domain

**Проблема:** API ключ или Domain могут быть неправильными.

**Проверка:**
- `config.php` строка 29: `MAILGUN_API_KEY` должен быть правильным
- `config.php` строка 30: `MAILGUN_DOMAIN` должен быть правильным

---

## 🔍 Как проверить

### Шаг 1: Проверьте, что бронирование создается

1. Создайте тестовое бронирование через форму на странице комнаты
2. Проверьте базу данных:
   - Таблица `bookings` - должна быть новая запись
   - Статус: `pending`
3. Если записи нет - бронирование не создается, значит API не вызывается

---

### Шаг 2: Проверьте логи Mailgun

1. Mailgun Dashboard → **Sending** → **Logs**
2. Если там пусто - письма не отправляются
3. Если есть записи с ошибками - проверьте ошибки

---

### Шаг 3: Проверьте логи PHP

1. Проверьте логи ошибок PHP на хостинге
2. Ищите сообщения типа:
   - "Mailgun API key is not configured"
   - "Mailgun domain is not configured"
   - "Mailgun API error"

---

### Шаг 4: Проверьте настройки Mailgun

1. Проверьте, что API ключ правильный в `config.php`
2. Проверьте, что Domain правильный в `config.php`
3. Проверьте, что email отправителя правильный

---

## 🧪 Тестирование отправки

### Тест 1: Создайте бронирование правильно

1. Откройте страницу комнаты: `https://new.backtobase.ca/room-basement.html`
2. Заполните форму бронирования:
   - Выберите даты
   - Укажите имя, email, телефон
   - Email должен быть в списке авторизованных в Mailgun
3. Нажмите "Book"
4. Должен произойти редирект на `booking-confirmation.html`
5. Проверьте Mailgun Dashboard → **Sending** → **Logs**

---

### Тест 2: Проверьте через API напрямую

Можно создать тестовый PHP файл для проверки отправки:

```php
<?php
require_once 'config.php';
require_once 'email_service.php';

$result = sendEmail(
    'backtobasewellness@gmail.com', // Используйте авторизованный email
    'Test Email',
    '<h1>Test</h1><p>This is a test email.</p>',
    'This is a test email.'
);

echo json_encode($result, JSON_PRETTY_PRINT);
?>
```

---

## ⚠️ Частые проблемы

### Проблема 1: "Mailgun API key is not configured"

**Причина:** `MAILGUN_API_KEY` пустой в `config.php`

**Решение:** Проверьте `config.php` строка 29

---

### Проблема 2: "Mailgun domain is not configured"

**Причина:** `MAILGUN_DOMAIN` пустой в `config.php`

**Решение:** Проверьте `config.php` строка 30

---

### Проблема 3: "Sender address not verified"

**Причина:** Email отправителя не в списке авторизованных

**Решение:** Добавьте `backtobasewellness@gmail.com` в список авторизованных получателей

---

### Проблема 4: "Recipient address not verified"

**Причина:** Email получателя не в списке авторизованных (для Sandbox Domain)

**Решение:** Добавьте email получателя в список авторизованных получателей

---

## 📝 Чек-лист проверки

- [ ] API ключ Mailgun добавлен в `config.php`
- [ ] Domain Mailgun добавлен в `config.php`
- [ ] Email отправителя добавлен в список авторизованных
- [ ] Email получателя добавлен в список авторизованных (для Sandbox)
- [ ] Бронирование создается через форму на странице комнаты
- [ ] Запись появилась в таблице `bookings` в базе данных
- [ ] Проверены логи Mailgun Dashboard
- [ ] Проверены логи PHP на хостинге

---

## 🔧 Что делать сейчас

1. **Проверьте `config.php`:**
   - Убедитесь, что `MAILGUN_API_KEY` заполнен
   - Убедитесь, что `MAILGUN_DOMAIN` заполнен

2. **Создайте тестовое бронирование правильно:**
   - Откройте страницу комнаты
   - Заполните форму
   - Нажмите "Book"

3. **Проверьте логи:**
   - Mailgun Dashboard → **Sending** → **Logs**
   - Логи PHP на хостинге

4. **Проверьте список авторизованных:**
   - Mailgun Dashboard → **Sending** → **Domain Settings** → **Authorized Recipients**

---

## ✅ Если все настроено правильно

Если все настройки правильные, но письма все равно не отправляются:

1. Проверьте, что файлы `config.php` и `email_service.php` загружены на хостинг
2. Проверьте версию файлов на хостинге (может быть старая версия)
3. Проверьте логи ошибок PHP на хостинге
4. Создайте тестовый PHP файл для прямой проверки отправки

**Нужна помощь с конкретной проверкой?** Скажите, что вы уже проверили, и я помогу найти проблему! 🚀



