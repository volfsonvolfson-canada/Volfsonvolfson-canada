# 🔍 Проверка: Какой домен используется для отправки

## 📊 Текущая ситуация

У вас есть 2 домена в Mailgun:

1. **`new.backtobase.ca`** - ваш верифицированный домен ✅
2. **`sandbox2856f0aed70c40a8b13e5a5f4f52c3bc.mailgun.org`** - Sandbox Domain (для тестирования)

---

## ⚠️ Проблема

**В `config.php` сейчас используется Sandbox Domain:**

```php
define('MAILGUN_DOMAIN', 'sandbox2856f0aed70c40a8b13e5a5f4f52c3bc.mailgun.org');
```

**Но у вас есть верифицированный домен `new.backtobase.ca`!**

---

## ✅ Решение: Использовать верифицированный домен

### Преимущества верифицированного домена:

1. ✅ **Можно отправлять на любые email-адреса** (без ограничений Sandbox)
2. ✅ **Лучшая доставляемость** (не попадает в спам)
3. ✅ **Профессионально** - письма от `@new.backtobase.ca`
4. ✅ **Клиенты НЕ должны подтверждать email**

---

## 🔧 Что нужно сделать

### Обновить `config.php`:

Замените Sandbox Domain на верифицированный домен:

```php
// Было:
define('MAILGUN_DOMAIN', 'sandbox2856f0aed70c40a8b13e5a5f4f52c3bc.mailgun.org');

// Станет:
define('MAILGUN_DOMAIN', 'new.backtobase.ca');
```

Также обновите email отправителя (опционально, но рекомендуется):

```php
// Можно использовать email на вашем домене:
define('MAILGUN_FROM_EMAIL', 'bookings@new.backtobase.ca'); // ✅ Используйте email на вашем домене
```

---

## 📝 Проверка верификации домена

### Перед переключением проверьте:

1. В Mailgun Dashboard → **Sending** → **Domains**
2. Найдите домен `new.backtobase.ca`
3. Проверьте статус:
   - ✅ **Verified** - домен верифицирован и готов к использованию
   - ⏳ **Pending** - еще не верифицирован (нужно подождать)
   - ⚠️ **Unverified** - DNS записи не найдены или неправильные

**Если статус Verified** - можно сразу переключаться!

**Если статус Pending или Unverified** - нужно сначала завершить верификацию.

---

## 🎯 Что делать сейчас

### Вариант 1: Если домен `new.backtobase.ca` верифицирован

1. Обновите `config.php`:
   - Замените `MAILGUN_DOMAIN` на `new.backtobase.ca`
   - Обновите `MAILGUN_FROM_EMAIL` на `bookings@new.backtobase.ca` (опционально)

2. Загрузите обновленный `config.php` на хостинг

3. Протестируйте отправку писем

---

### Вариант 2: Если домен еще не верифицирован

1. Проверьте статус домена в Mailgun
2. Если DNS записи добавлены, но статус еще Pending - подождите верификации
3. После верификации обновите `config.php`

---

## ⚠️ Почему письма не приходят с Sandbox Domain

**Sandbox Domain может иметь проблемы с доставляемостью:**

1. Письма могут попадать в спам чаще
2. Некоторые провайдеры могут блокировать письма от Sandbox Domain
3. Ограничения по количеству получателей

**Верифицированный домен `new.backtobase.ca` будет работать лучше!**

---

## 🔍 Как проверить, какой домен используется

### В Mailgun Dashboard → Sending → Logs:

1. Откройте детали отправки
2. Посмотрите поле **"From"** или **"Domain"**
3. Там будет указан домен, который использовался для отправки

**Если там `sandbox2856f0aed70c40a8b13e5a5f4f52c3bc.mailgun.org`** - используем Sandbox  
**Если там `new.backtobase.ca`** - используем верифицированный домен

---

## ✅ Итог

**Проблема:** Используется Sandbox Domain вместо верифицированного домена

**Решение:** 
1. Проверьте статус домена `new.backtobase.ca` в Mailgun (должен быть Verified)
2. Обновите `MAILGUN_DOMAIN` в `config.php` на `new.backtobase.ca`
3. Загрузите обновленный `config.php` на хостинг
4. Протестируйте отправку

**После переключения на верифицированный домен письма будут доставляться лучше!** 🚀



