# 🔧 Исправление: Две DMARC записи и неправильное значение

## ❌ Проблемы

1. **ДВЕ TXT записи для `_dmarc`** - в MXToolbox видно две записи
2. **Значение не совпадает** - на хостинге есть дополнительные параметры

---

## 🔍 Что видно в MXToolbox

**Запись 1 (старая/неправильная):**
```
v=DMARC1; p=none
```

**Запись 2 (новая, но не полная):**
```
v=DMARC1; p=none; pct=100; fo=1; ri=3600; rua=mailto:ca560f54@dmarc.mailgun.org,mailto:6025edc5@inbox.ondmarc.com; ruf=mailto:ca560f54@dmarc.mailgun.org,mailto:6025edc5@inbox.ondmarc.com;
```

---

## 🔍 Что на хостинге

**Значение на хостинге:**
```
v=DMARC1;p=none;sp=none;adkim=r;aspf=r;pct=100;fo=1;rf=afrf;ri=3600;rua=mailto:ca560f54@dmarc.mailgun.org,mailto:6025edc5@inbox.ondmarc.com;ruf=mailto:ca560f54@dmarc.mailgun.org,mailto:6025edc5@inbox.ondmarc.com
```

**Проблемы:**
- Есть дополнительные параметры: `sp=none;adkim=r;aspf=r;rf=afrf`
- Mailgun требует точное значение без этих параметров

---

## ✅ Что нужно сделать

### Шаг 1: Найти все DMARC записи в Zone Editor

**В Zone Editor:**

1. Используйте фильтр "TXT" чтобы увидеть только TXT записи
2. Найдите **ВСЕ** записи с именем `_dmarc` (их может быть несколько)
3. **Запишите, сколько их есть**

---

### Шаг 2: Удалить все старые DMARC записи

**Для каждой записи `_dmarc`:**

1. Нажмите кнопку **"Delete"** (красная, с иконкой корзины)
2. Подтвердите удаление

**Удалите ВСЕ записи `_dmarc`!**

---

### Шаг 3: Создать одну новую правильную запись

**После удаления всех старых записей:**

1. Нажмите **"Add Record"** → **"TXT"**
2. Заполните:
   - **Name:** `_dmarc` (без домена!)
   - **TTL:** `3600`
   - **TXT Data:** Вставьте **ТОЧНО** это значение (без дополнительных параметров):
     ```
     v=DMARC1; p=none; pct=100; fo=1; ri=3600; rua=mailto:ca560f54@dmarc.mailgun.org,mailto:6025edc5@inbox.ondmarc.com; ruf=mailto:ca560f54@dmarc.mailgun.org,mailto:6025edc5@inbox.ondmarc.com;
     ```
3. **Сохраните**

**Важно:**
- Используйте значение **ТОЧНО** как указано выше
- Без параметров `sp=none;adkim=r;aspf=r;rf=afrf`
- С пробелами после точек с запятой (как в примере выше)

---

## 📋 Правильное значение для DMARC

**Точное значение, которое нужно использовать:**

```
v=DMARC1; p=none; pct=100; fo=1; ri=3600; rua=mailto:ca560f54@dmarc.mailgun.org,mailto:6025edc5@inbox.ondmarc.com; ruf=mailto:ca560f54@dmarc.mailgun.org,mailto:6025edc5@inbox.ondmarc.com;
```

**Параметры:**
- `v=DMARC1` - версия DMARC
- `p=none` - политика (none = не блокировать)
- `pct=100` - процент сообщений
- `fo=1` - режим отчета
- `ri=3600` - интервал отчетов
- `rua=...` - email для агрегированных отчетов
- `ruf=...` - email для отчетов о сбоях

**НЕ добавляйте:**
- `sp=none`
- `adkim=r`
- `aspf=r`
- `rf=afrf`

---

## ✅ После исправления

1. **Подождите 5-30 минут** для распространения DNS
2. **Проверьте через MXToolbox:**
   - https://mxtoolbox.com/TXTLookup.aspx
   - Введите: `_dmarc.new.backtobase.ca`
   - Должна быть **ОДНА** запись с правильным значением
3. **Вернитесь в Mailgun:**
   - Обновите страницу
   - Mailgun должен автоматически проверить запись
   - Статус должен измениться на "Verified"

---

## 🎯 Итог

**Что нужно сделать:**

1. ✅ Найти **ВСЕ** записи `_dmarc` в Zone Editor
2. ✅ Удалить **ВСЕ** старые записи
3. ✅ Создать **ОДНУ** новую запись с правильным значением (без дополнительных параметров)
4. ✅ Подождать распространения DNS
5. ✅ Проверить через MXToolbox (должна быть одна запись)
6. ✅ Вернуться в Mailgun и дождаться верификации

**Сообщите, когда исправите запись и проверите через MXToolbox!** 🚀



