# 📋 DNS записи для new.backtobase.ca в Mailgun

## 📊 Записи, которые нужно добавить

У вас есть 2 записи для добавления:

---

## 1️⃣ TXT запись для DKIM (подпись писем)

**Тип:** TXT  
**Хост (имя):** `krs._domainkey.new`  
**Значение:** 
```
k=rsa; p=
```

**Важно:** 
- В некоторых панелях хостинга нужно указывать полное имя: `krs._domainkey.new.backtobase.ca`
- В других - только `krs._domainkey.new`
- Попробуйте сначала короткий вариант, если не работает - используйте полный

---

## 2️⃣ TXT запись для SPF (верификация отправителя)

**Тип:** TXT  
**Хост (имя):** `new` (или `@` для корневого домена, но у вас поддомен)  
**Значение:**
```
v=spf1 include:mailgun.org ~all
```

**Важно:**
- Для поддомена `new.backtobase.ca` обычно используется `new` в качестве имени
- Если не работает, попробуйте `new.backtobase.ca` (полное имя)

---

## 3️⃣ MX записи (опционально)

**MX записи нужны только если вы хотите получать почту через Mailgun.**

Если вы **НЕ планируете** получать почту через Mailgun (только отправлять):
- **Не добавляйте MX записи**
- Пропустите этот шаг

Если вы **хотите** получать почту через Mailgun:
- Mailgun должен показать MX записи
- Обычно это что-то вроде:
  - `mxa.mailgun.org` (приоритет 10)
  - `mxb.mailgun.org` (приоритет 10)

---

## 🔧 Как добавить записи на хостинге

### Шаг 1: Зайти в панель управления DNS

1. Войдите в панель управления хостингом
2. Найдите раздел **DNS** или **Domain Management**
3. Выберите домен `new.backtobase.ca` (или `backtobase.ca` с поддоменом `new`)

### Шаг 2: Добавить TXT запись для DKIM

1. Нажмите **Add Record** или **Добавить запись**
2. Выберите тип: **TXT**
3. В поле **Name** или **Host** введите: `krs._domainkey.new`
   - Если не работает, попробуйте: `krs._domainkey.new.backtobase.ca`
4. В поле **Value** или **Data** вставьте:
   ```
   k=rsa; p=
   ```
5. TTL: оставьте по умолчанию (обычно 3600 или 14400)
6. Сохраните

### Шаг 3: Добавить TXT запись для SPF

1. Нажмите **Add Record** или **Добавить запись**
2. Выберите тип: **TXT**
3. В поле **Name** или **Host** введите: `new`
   - Если не работает, попробуйте: `new.backtobase.ca`
4. В поле **Value** или **Data** вставьте:
   ```
   v=spf1 include:mailgun.org ~all
   ```
5. TTL: оставьте по умолчанию
6. Сохраните

---

## ⚠️ Важные моменты

### Для поддомена `new.backtobase.ca`:

1. **Имя записи:**
   - В некоторых панелях нужно указывать только `new`
   - В других - `new.backtobase.ca`
   - Попробуйте сначала короткий вариант

2. **DKIM запись:**
   - Имя: `krs._domainkey.new` (или `krs._domainkey.new.backtobase.ca`)
   - Значение: длинная строка (скопируйте полностью)

3. **SPF запись:**
   - Имя: `new` (или `new.backtobase.ca`)
   - Значение: `v=spf1 include:mailgun.org ~all`

---

## ✅ Проверка после добавления

### 1. Проверить в Mailgun:

1. В Mailgun Dashboard → **Sending** → **Domains**
2. Найдите `new.backtobase.ca`
3. Нажмите **Verify DNS Settings**
4. Mailgun проверит записи и покажет статус

### 2. Проверить через онлайн-инструменты:

1. Используйте https://mxtoolbox.com
2. Введите `new.backtobase.ca`
3. Выберите **TXT Lookup**
4. Проверьте, что записи отображаются

---

## ⏳ Время ожидания

После добавления DNS записей:

1. **Распространение DNS:** 1-24 часа (обычно 1-4 часа)
2. **Верификация Mailgun:** автоматически после распространения DNS
3. **Общее время:** обычно 2-48 часов

---

## 🔄 Что делать после верификации

Когда домен будет верифицирован (статус **Verified** в Mailgun):

1. Обновите `config.php`:
```php
define('MAILGUN_DOMAIN', 'new.backtobase.ca'); // ✅ После верификации
```

2. Обновите email отправителя:
```php
define('MAILGUN_FROM_EMAIL', 'bookings@new.backtobase.ca'); // ✅ Используйте email на вашем домене
```

3. Загрузите обновленный `config.php` на хостинг

---

## 📝 Итог

**Что нужно добавить:**
1. ✅ TXT запись для DKIM: `krs._domainkey.new` → (длинное значение)
2. ✅ TXT запись для SPF: `new` → `v=spf1 include:mailgun.org ~all`
3. ⏭️ MX записи (опционально, только если нужна входящая почта)

**После добавления:**
- Подождите 1-48 часов для распространения DNS
- Mailgun автоматически проверит записи
- После верификации обновите `config.php`

**Нужна помощь с конкретной панелью хостинга?** Дайте знать, какая у вас панель (cPanel, Plesk, и т.д.), и я помогу с точными инструкциями! 🚀



