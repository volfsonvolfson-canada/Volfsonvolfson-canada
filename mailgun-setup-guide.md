# 🚀 Инструкция по настройке Mailgun

## 📋 Что нужно для интеграции

### 1. **Mailgun API Key** (обязательно)
- Ключ для отправки email через Mailgun API
- Получить можно в Mailgun Dashboard → Sending → API Keys

### 2. **Mailgun Domain** (обязательно)
- Для тестирования: Sandbox Domain (например, `sandbox123.mailgun.org`)
- Для продакшена: ваш собственный домен (например, `backtobase.ca`)

### 3. **Email отправителя**
- Email, с которого будут отправляться письма
- Должен быть в вашем Mailgun Domain

### 4. **Email владельца**
- Ваш email для уведомлений о новых бронированиях

---

## 🎯 Быстрая настройка (3 шага)

### Шаг 1: Регистрация и получение Sandbox Domain

1. Перейдите на https://www.mailgun.com
2. Нажмите **Sign Up** (или **Login**, если уже зарегистрированы)
3. Заполните форму регистрации:
   - Email (можно использовать любой)
   - Пароль
   - Название компании: `Back to Base Hotel`
4. После регистрации вы автоматически получите **Sandbox Domain**
   - Выглядит примерно так: `sandbox123.mailgun.org`
   - ⚠️ **Важно**: Sandbox Domain работает только для **верифицированных email-адресов**
   - Вам нужно будет добавить свой email в список разрешенных отправителей

---

### Шаг 2: Добавить разрешенный email (для Sandbox)

1. В Mailgun Dashboard перейдите в **Sending** → **Domain Settings**
2. Выберите ваш Sandbox Domain
3. Перейдите на вкладку **Authorized Recipients**
4. Нажмите **Add Recipient**
5. Введите email, который вы хотите использовать как отправитель (например, `bookings@backtobase.ca` или ваш личный email)
6. Mailgun отправит письмо с подтверждением
7. Откройте письмо и нажмите на ссылку подтверждения
8. ✅ Email будет добавлен в список разрешенных

⚠️ **Важно для Sandbox**: Вы можете отправлять письма **только** на email-адреса, которые были добавлены в список разрешенных получателей. Это ограничение Sandbox Domain.

---

### Шаг 3: Получить API Key

1. В Mailgun Dashboard перейдите в **Sending** → **API Keys**
2. Найдите ключ **Private API key** (или **Public API key** для тестирования)
3. Нажмите **Show** или **Reveal** чтобы увидеть ключ
4. **Скопируйте API Key**
   - Выглядит примерно так: `xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx-xxxxxxxx-xxxxxxxx`
   - ⚠️ **Сохраните ключ сразу** - если потеряете, можно создать новый

---

### Шаг 4: Добавить данные в config.php

Откройте файл `config.php` на хостинге и обновите следующие строки:

```php
// Строка 29: Вставьте ваш Mailgun API Key
define('MAILGUN_API_KEY', 'ваш_скопированный_ключ');

// Строка 30: Вставьте ваш Mailgun Domain
define('MAILGUN_DOMAIN', 'sandbox123.mailgun.org'); // ⚠️ Замените на ваш Sandbox Domain

// Строка 31: Email отправителя (должен быть в списке разрешенных для Sandbox)
define('MAILGUN_FROM_EMAIL', 'bookings@backtobase.ca'); // ⚠️ Замените на ваш разрешенный email

// Строка 33: Ваш email для получения уведомлений о новых бронированиях
define('MAILGUN_HOST_EMAIL', 'ваш_email@example.com'); // ⚠️ Замените на ваш email
```

---

## 📝 Для продакшена: Добавление собственного домена

Когда будете готовы к продакшену:

1. В Mailgun Dashboard перейдите в **Sending** → **Domains**
2. Нажмите **Add New Domain**
3. Введите ваш домен (например, `backtobase.ca`)
4. Выберите тип: **EU** или **US** (рекомендую US для Канады)
5. Mailgun предоставит DNS записи для добавления:
   - TXT записи для верификации
   - MX записи (опционально)
   - CNAME записи
6. Добавьте эти записи в DNS вашего домена
7. После верификации домен будет готов к использованию
8. Обновите `MAILGUN_DOMAIN` в `config.php` на ваш домен

---

## ✅ Проверка настройки

После добавления данных:

1. Сохраните `config.php`
2. Создайте тестовое бронирование на сайте
3. Проверьте:
   - В Mailgun Dashboard → **Sending** → **Logs** должны появиться отправки
   - Проверьте почтовый ящик получателя (и папку Spam)

---

## 🔍 Где найти данные в Mailgun

### API Key:
- **Sending** → **API Keys** → нажмите **Show** или **Reveal**

### Domain:
- **Sending** → **Domains** → там будет ваш Sandbox Domain или добавленные домены

### Разрешенные получатели (для Sandbox):
- **Sending** → **Domain Settings** → выберите домен → вкладка **Authorized Recipients**

---

## ⚠️ Важные ограничения Sandbox Domain

1. **Только разрешенные получатели**: 
   - Вы можете отправлять письма **только** на email-адреса, которые были добавлены в список разрешенных
   - Это ограничение Sandbox Domain
   - Для продакшена нужно добавить свой домен

2. **Лимиты**:
   - Бесплатный тариф: 5,000 писем/месяц (первые 3 месяца), затем 1,000/месяц
   - Платные планы от $35/месяц (50,000+ писем)

3. **Отправитель должен быть в списке разрешенных**:
   - Email в `MAILGUN_FROM_EMAIL` должен быть добавлен в список разрешенных отправителей

---

## ⚠️ Частые проблемы и решения

### Проблема 1: Письма не отправляются
- **Решение**: 
  - Проверьте, что email отправителя добавлен в список разрешенных (для Sandbox)
  - Проверьте, что API Key правильный и активен
  - Проверьте логи в Mailgun Dashboard → Sending → Logs

### Проблема 2: Ошибка 403 Forbidden
- **Решение**: 
  - Проверьте, что API Key правильный
  - Проверьте, что Domain правильный
  - Убедитесь, что используете Private API Key (не Public)

### Проблема 3: Ошибка "Sender address not verified"
- **Решение**: 
  - Добавьте email отправителя в список разрешенных (для Sandbox)
  - Или добавьте и верифицируйте свой домен (для продакшена)

### Проблема 4: Письма попадают в спам
- **Решение**: 
  - Используйте верифицированный домен (не Sandbox)
  - Настройте SPF/DKIM записи в DNS
  - Mailgun предоставляет инструкции для настройки DNS

---

## 📊 Структура файлов

После интеграции Mailgun у вас будут следующие файлы:

- ✅ `config.php` - содержит `MAILGUN_API_KEY`, `MAILGUN_DOMAIN`, `MAILGUN_FROM_EMAIL`, `MAILGUN_HOST_EMAIL`
- ✅ `email_service.php` - обновлен для работы с Mailgun API
- ✅ Все остальные файлы без изменений (используют функции из `email_service.php`)

---

## 🎉 Готово!

После выполнения всех шагов:

1. ✅ Mailgun настроен
2. ✅ Код обновлен для работы с Mailgun
3. ✅ Система отправки email готова к использованию

**Следующие шаги:**
1. Протестируйте отправку тестового письма
2. Создайте тестовое бронирование
3. Проверьте, что письма доставляются

Если возникнут проблемы - дайте знать! 🚀



