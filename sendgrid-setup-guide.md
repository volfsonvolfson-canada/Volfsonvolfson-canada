# Инструкция по настройке SendGrid

## 📋 Что нужно для интеграции

### 1. **SendGrid API Key** (главное, что нужно)
- Это ключ для отправки email через SendGrid API
- Нужен для файла `config.php`
- Получить можно в SendGrid Dashboard → Settings → API Keys

### 2. **Верифицированный отправитель (Sender)**
- Email адрес, с которого будут отправляться письма
- Нужно верифицировать в SendGrid (подтвердить владение email)
- Будет использоваться для поля "From" в письмах

### 3. **Отображаемое имя отправителя**
- Имя, которое будет показываться в поле "From" (например, "Back to Base")

---

## 🚀 Пошаговая настройка

### Шаг 1: Верификация отправителя (Sender Verification)

1. Войдите в **SendGrid Dashboard**: https://app.sendgrid.com
2. Перейдите в **Settings** → **Sender Authentication**
3. Выберите **Single Sender Verification** (для начала, самый простой вариант)
4. Нажмите **Create New Sender**
5. Заполните форму:
   - **From Email**: Ваш email (например, `hello@backtobase.ca` или `noreply@backtobase.ca`)
   - **From Name**: Отображаемое имя (например, `Back to Base`)
   - **Reply To**: Email для ответов (может быть тот же)
   - **Company Address**: Адрес вашей компании
   - **City**: Город
   - **State**: Провинция (например, BC для British Columbia)
   - **Country**: Canada
   - **Zip Code**: Почтовый индекс
6. Нажмите **Create**
7. **Проверьте email** - SendGrid отправит письмо с подтверждением
8. Откройте письмо и нажмите на ссылку подтверждения
9. ✅ После подтверждения статус изменится на **Verified**

⚠️ **Важно**: Без верифицированного отправителя SendGrid не будет отправлять письма!

---

### Шаг 2: Создание API Key

1. В SendGrid Dashboard перейдите в **Settings** → **API Keys**
2. Нажмите **Create API Key**
3. Введите название ключа (например, `Back to Base Booking System`)
4. Выберите права доступа:
   - **Full Access** (для простоты) - или
   - **Restricted Access** → выберите только **Mail Send** (рекомендуется для безопасности)
5. Нажмите **Create & View**
6. **Скопируйте API Key** - он показывается только один раз!
   - Выглядит примерно так: `SG.xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`
7. Сохраните ключ в безопасном месте

⚠️ **Важно**: 
- API Key показывается только один раз при создании
- Если потеряли, нужно создать новый ключ
- Никогда не публикуйте API Key в открытом доступе

---

### Шаг 3: Добавление ключа в config.php

1. Откройте файл `config.php` на хостинге
2. Найдите строку с `SENDGRID_API_KEY`
3. Вставьте ваш API Key:

```php
// Email Service Configuration (SendGrid)
define('SENDGRID_API_KEY', 'SG.ваш_скопированный_ключ_здесь');
define('SENDGRID_FROM_EMAIL', 'hello@backtobase.ca'); // Верифицированный email
define('SENDGRID_FROM_NAME', 'Back to Base'); // Отображаемое имя
define('HOST_EMAIL', 'owner@backtobase.ca'); // Email владельца (для уведомлений о новых бронированиях)
```

4. Сохраните файл

---

### Шаг 4: Проверка настройки

1. После добавления API Key система должна начать отправлять письма
2. Создайте тестовое бронирование через сайт
3. Проверьте, что письма отправляются:
   - В SendGrid Dashboard → **Activity** можно увидеть все отправки
   - Проверьте почтовый ящик получателя

---

## 📧 Конфигурация для вашего проекта

### Рекомендуемые значения:

```php
// В config.php
define('SENDGRID_API_KEY', 'SG.ваш_ключ');
define('SENDGRID_FROM_EMAIL', 'hello@backtobase.ca'); // Или другой верифицированный email
define('SENDGRID_FROM_NAME', 'Back to Base');
define('HOST_EMAIL', 'ваш_email@example.com'); // Email владельца для уведомлений
```

---

## 🔍 Как найти нужные данные в SendGrid

### API Key:
1. Dashboard → **Settings** → **API Keys**
2. Нажмите на название существующего ключа или создайте новый
3. Если ключ уже создан, но не знаете его - создайте новый (старый можно деактивировать)

### Верифицированный отправитель:
1. Dashboard → **Settings** → **Sender Authentication**
2. Раздел **Single Sender Verification**
3. Там будет список верифицированных отправителей
4. Используйте email из списка (статус должен быть **Verified**)

### Логи и статистика:
- Dashboard → **Activity** - история всех отправок
- Dashboard → **Stats** - статистика по отправкам

---

## ⚠️ Частые проблемы и решения

### Проблема 1: Письма не отправляются
- **Решение**: Проверьте, что отправитель верифицирован (Sender Verification)
- Проверьте, что API Key правильный и активен
- Проверьте логи ошибок в SendGrid Dashboard → Activity

### Проблема 2: Письма попадают в спам
- **Решение**: 
  - Используйте верифицированный email домена
  - Настройте SPF/DKIM записи (для Domain Authentication)
  - Для начала Single Sender Verification достаточно

### Проблема 3: Ошибка 403 Forbidden
- **Решение**: 
  - Проверьте права доступа API Key (должны быть включены "Mail Send")
  - Убедитесь, что API Key активен (не деактивирован)

### Проблема 4: Ошибка Invalid API Key
- **Решение**: 
  - Проверьте, что ключ скопирован полностью (без пробелов)
  - Проверьте, что ключ начинается с `SG.`
  - Создайте новый ключ, если старый не работает

---

## 🔐 Безопасность

1. **Никогда не публикуйте API Key** в открытом коде
2. **Используйте Restricted Access** для API Key (только Mail Send права)
3. **Регулярно проверяйте Activity** на подозрительную активность
4. **Храните ключ в config.php**, который не доступен публично

---

## 📝 Что дальше?

После настройки SendGrid:

1. ✅ Добавьте API Key в `config.php`
2. ✅ Проверьте отправку тестового письма
3. ✅ Создайте тестовое бронирование на сайте
4. ✅ Проверьте, что письма доставляются
5. ✅ Проверьте папку Spam, если письма не приходят

Если нужна помощь с конкретными шагами или возникли проблемы - дайте знать!



