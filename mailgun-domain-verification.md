# 🔐 Верификация домена new.backtobase.ca в Mailgun

## 📋 Текущая ситуация

✅ Домен `new.backtobase.ca` добавлен в Mailgun  
⏳ Верификация еще не завершена  
🔄 Пока используем Sandbox Domain для работы

---

## 🎯 Что нужно сделать для верификации

### Шаг 1: Получить DNS записи от Mailgun

1. В Mailgun Dashboard → **Sending** → **Domains**
2. Найдите ваш домен `new.backtobase.ca`
3. Нажмите на него (или кнопку **Verify DNS Settings**)
4. Mailgun покажет DNS записи, которые нужно добавить

**Обычно это:**
- **TXT запись** для верификации домена
- **TXT запись** для DKIM (подпись писем)
- **CNAME запись** (опционально, для отслеживания)

---

### Шаг 2: Добавить DNS записи на хостинге

1. Зайдите в панель управления хостингом
2. Найдите настройки DNS для домена `new.backtobase.ca`
3. Добавьте записи, которые предоставил Mailgun:

**Пример:**
```
TXT Record для верификации:
Name: @ (или new.backtobase.ca)
Value: v=spf1 include:mailgun.org ~all

TXT Record для DKIM:
Name: k1._domainkey.new (или k1._domainkey)
Value: k=rsa; p=MIGfMA0GCSqGSIb3DQEBAQUAA4GNADCBiQKBgQC... (длинная строка)

CNAME Record (если требуется):
Name: email
Value: mailgun.org
```

**Важно:**
- Если Mailgun показывает для поддомена `new` - используйте `new` в Name
- Если показывает для корневого домена - используйте `@` или оставьте пустым

---

### Шаг 3: Сохранить и подождать

1. Сохраните DNS записи на хостинге
2. Mailgun автоматически проверит DNS записи
3. Обычно это занимает **24-48 часов** (но может быть быстрее - от 1 часа)
4. Можно проверить статус в Mailgun Dashboard → **Sending** → **Domains**

---

## 🔄 Что делать пока домен не верифицирован

### Вариант 1: Использовать Sandbox Domain (текущая настройка)

**Пока домен не верифицирован, используйте Sandbox Domain:**

```php
// В config.php
define('MAILGUN_DOMAIN', 'sandbox2856f0aed70c40a8b13e5a5f4f52c3bc.mailgun.org'); // ✅ Текущая настройка
```

**Ограничения:**
- Можно отправлять только на авторизованные email-адреса
- Нужно добавлять получателей в список разрешенных

---

### Вариант 2: После верификации переключиться на домен

**Когда домен будет верифицирован:**

1. Обновите `config.php`:
```php
define('MAILGUN_DOMAIN', 'new.backtobase.ca'); // ✅ После верификации
```

2. Также обновите email отправителя:
```php
define('MAILGUN_FROM_EMAIL', 'bookings@new.backtobase.ca'); // ✅ Используйте email на вашем домене
```

3. Загрузите обновленный `config.php` на хостинг

**Преимущества:**
- ✅ Можно отправлять на любые email-адреса
- ✅ Клиенты НЕ должны подтверждать email
- ✅ Профессионально и автоматически

---

## 📊 Как проверить статус верификации

1. В Mailgun Dashboard → **Sending** → **Domains**
2. Найдите `new.backtobase.ca`
3. Посмотрите статус:
   - ⏳ **Pending** - ожидает верификации
   - ⚠️ **Unverified** - DNS записи не найдены или неправильные
   - ✅ **Verified** - домен верифицирован и готов к использованию

---

## ⚠️ Частые проблемы

### Проблема 1: DNS записи не найдены

**Причина:**
- DNS записи еще не распространились (подождите 1-24 часа)
- Неправильно добавлены записи

**Решение:**
- Проверьте, что записи добавлены правильно
- Подождите распространения DNS (может занять до 48 часов)
- Используйте инструменты проверки DNS (например, https://mxtoolbox.com)

### Проблема 2: Статус "Unverified"

**Причина:**
- DNS записи неправильные
- Неправильный формат Name в DNS записях

**Решение:**
- Проверьте точное соответствие записей тем, что показал Mailgun
- Убедитесь, что используется правильный формат для поддомена (например, `k1._domainkey.new`)

---

## ✅ Что делать сейчас

### 1. Получить DNS записи от Mailgun:
- Зайдите в Mailgun Dashboard → **Sending** → **Domains**
- Нажмите на `new.backtobase.ca`
- Скопируйте все DNS записи

### 2. Добавить DNS записи на хостинге:
- Зайдите в панель управления хостингом
- Найдите настройки DNS для `new.backtobase.ca`
- Добавьте записи, которые предоставил Mailgun

### 3. Пока ждете верификации:
- Используйте Sandbox Domain (текущая настройка работает)
- Тестируйте отправку писем на авторизованные email

### 4. После верификации:
- Обновите `MAILGUN_DOMAIN` в `config.php` на `new.backtobase.ca`
- Обновите `MAILGUN_FROM_EMAIL` на email на вашем домене
- Загрузите обновленный `config.php` на хостинг

---

## 🎯 Итог

**Текущая ситуация:**
- ✅ Sandbox Domain работает
- ⏳ Домен `new.backtobase.ca` добавлен, но еще не верифицирован

**Что делать:**
1. Добавьте DNS записи от Mailgun на хостинге
2. Подождите верификации (24-48 часов)
3. После верификации обновите `config.php`

**Нужна помощь с добавлением DNS записей?** Дайте знать, какие записи показал Mailgun, и я помогу их правильно добавить! 🚀



