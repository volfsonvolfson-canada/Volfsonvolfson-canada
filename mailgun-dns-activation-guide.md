# 🔧 Mailgun: Активация DNS записей для верификации домена

## 📋 Что видно на скриншоте

**Статус:** DKIM ключ не верифицирован (`Unverified`)

**Инструкция от Mailgun:**
> "Install the TXT record with your DNS provider and then click "Activate" in the adjacent menu."

**Это означает:**
1. Нужно добавить DNS записи в хостинг (DNS провайдер)
2. Подождать распространения DNS (обычно 5-30 минут)
3. Нажать "Activate" в Mailgun

---

## 🔍 Что нужно сделать

### Шаг 1: Получить DNS записи из Mailgun

1. **В Mailgun Dashboard** найдите раздел с DNS записями для домена `new.backtobase.ca`
2. **Скопируйте все TXT записи**, которые нужно добавить:
   - **DKIM записи** (обычно 2-3 записи вида `mailo._domainkey.new.backtobase.ca`)
   - **SPF запись** (обычно `@` или `new.backtobase.ca`)
   - **MX записи** (если нужны)

**Пример того, что должно быть:**
```
Type: TXT
Name: mailo._domainkey
Value: k=rsa; p=MIGfMA0GCSqGSIb3DQEBAQUAA4GNADCBiQKBgQC...
```

```
Type: TXT
Name: @
Value: v=spf1 include:mailgun.org ~all
```

---

### Шаг 2: Добавить DNS записи в хостинг (GreenGeeks)

**Через cPanel:**

1. **Войдите в cPanel**
2. Найдите раздел **"Zone Editor"** или **"DNS Zone Editor"**
3. Выберите домен **`new.backtobase.ca`**
4. Нажмите **"Add Record"** или **"Add"**
5. Для каждой записи:
   - **Type:** `TXT`
   - **Name:** (например, `mailo._domainkey` или `@`)
   - **Value:** (скопируйте из Mailgun)
   - **TTL:** `3600` (или оставьте по умолчанию)
6. Нажмите **"Add Record"** или **"Save"**

**Повторите для всех записей из Mailgun!**

---

### Шаг 3: Проверить распространение DNS

**После добавления записей подождите 5-30 минут** для распространения DNS.

**Проверить можно через:**
- https://mxtoolbox.com/TXTLookup.aspx
- https://www.whatsmydns.net/

**Или через командную строку:**
```bash
nslookup -type=TXT mailo._domainkey.new.backtobase.ca
nslookup -type=TXT new.backtobase.ca
```

---

### Шаг 4: Активировать в Mailgun

**После того, как DNS записи распространились:**

1. **Вернитесь в Mailgun Dashboard**
2. Найдите раздел с DNS записями для домена `new.backtobase.ca`
3. **Рядом с каждой записью должна быть кнопка "Activate"** или меню
4. **Нажмите "Activate"** для каждой записи
5. Mailgun проверит DNS записи
6. Если все правильно, статус изменится на **"Verified"** (зеленый)

---

## ⚠️ Важно

### Если записи уже были добавлены ранее

**Если вы уже добавляли DNS записи ранее:**
1. Проверьте, что они все еще есть в хостинге
2. Проверьте, что значения правильные (совпадают с Mailgun)
3. Если записи есть, но Mailgun показывает "Unverified":
   - Подождите еще немного (DNS может распространяться до 48 часов)
   - Проверьте, что значения точно совпадают (включая пробелы)
   - Нажмите "Activate" в Mailgun

---

## 🔍 Проверка DNS записей

### Проверить через онлайн сервисы:

1. **MXToolbox:**
   - https://mxtoolbox.com/TXTLookup.aspx
   - Введите: `mailo._domainkey.new.backtobase.ca`
   - Должна показаться TXT запись с DKIM ключом

2. **What's My DNS:**
   - https://www.whatsmydns.net/
   - Введите домен и выберите TXT

---

## 📋 Что делать сейчас

### 1. Скопируйте DNS записи из Mailgun

**В Mailgun Dashboard:**
- Найдите раздел **"DNS Records"** или **"Domain Settings"**
- Скопируйте все TXT записи, которые нужно добавить

---

### 2. Добавьте записи в хостинг (если еще не добавлены)

**Через cPanel → Zone Editor:**
- Добавьте все TXT записи из Mailgun
- Сохраните

---

### 3. Проверьте распространение DNS

**Подождите 5-30 минут** и проверьте через MXToolbox или whatsmydns.net

---

### 4. Активируйте в Mailgun

**После проверки DNS:**
- Нажмите **"Activate"** в Mailgun для каждой записи
- Дождитесь верификации
- Статус должен измениться на **"Verified"**

---

## 🎯 После верификации домена

**После того, как домен будет верифицирован:**

1. ✅ Можно отправлять на **любые** email адреса (не нужно добавлять в Authorized Recipients)
2. ✅ Лучшая доставляемость писем
3. ✅ Профессиональный подход
4. ✅ Можно использовать `bookings@new.backtobase.ca` как адрес отправителя

---

## 📞 Если не получается

**Если DNS записи не распространяются или не верифицируются:**

1. **Проверьте значения:**
   - Убедитесь, что значения точно совпадают с Mailgun
   - Проверьте пробелы и специальные символы
   - DKIM записи могут быть очень длинными - убедитесь, что скопированы полностью

2. **Проверьте TTL:**
   - Установите TTL на 3600 (1 час) для быстрого распространения

3. **Обратитесь в поддержку хостинга:**
   - Спросите, правильно ли добавлены DNS записи
   - Попросите проверить, что записи видны с их серверов

---

## 🚀 Итог

**Шаги для верификации:**
1. ✅ Скопировать DNS записи из Mailgun
2. ✅ Добавить в хостинг (cPanel → Zone Editor)
3. ✅ Подождать распространения DNS (5-30 минут)
4. ✅ Нажать "Activate" в Mailgun
5. ✅ Дождаться верификации

**После верификации домен будет готов к использованию!** 🎉

**Сообщите, какие DNS записи показывает Mailgun, и я помогу добавить их правильно!**



