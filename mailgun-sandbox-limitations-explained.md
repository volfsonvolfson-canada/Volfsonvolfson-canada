# 📧 Как работает Mailgun Sandbox Domain с клиентами

## ❌ Важно: Клиенты НЕ подтверждают email в Mailgun

**Клиенты НЕ должны регистрироваться или подтверждать что-то в Mailgun!**

---

## 🔍 Как работает Sandbox Domain

### Ограничение Sandbox Domain:

**Mailgun Sandbox Domain может отправлять письма ТОЛЬКО на email-адреса, которые ВЫ (администратор) добавили в список разрешенных получателей.**

### Процесс:

1. **Клиент создает бронирование** на вашем сайте
2. **Система пытается отправить письмо** клиенту на его email
3. **Mailgun проверяет**: есть ли этот email в списке разрешенных?
   - ✅ **Если ДА** → письмо отправляется
   - ❌ **Если НЕТ** → письмо НЕ отправляется (ошибка)

---

## ⚠️ Проблема с текущей настройкой

### С Sandbox Domain:

**Вы не можете отправлять письма на email-адреса случайных клиентов!**

Каждый раз, когда новый клиент хочет получить письмо, вам нужно:
1. Зайти в Mailgun Dashboard
2. Добавить его email в список разрешенных
3. Подождать, пока он подтвердит email (Mailgun отправит ему письмо)
4. Только после этого система сможет отправить ему письмо о бронировании

**Это неудобно для продакшена!**

---

## ✅ Решения

### Вариант 1: Добавить свой домен (РЕКОМЕНДУЮ) ⭐

**Для продакшена нужно добавить и верифицировать свой домен `backtobase.ca`:**

1. В Mailgun Dashboard → **Sending** → **Domains**
2. Нажмите **Add New Domain**
3. Введите `backtobase.ca`
4. Mailgun предоставит DNS записи для добавления:
   - TXT записи для верификации
   - CNAME записи
5. Добавьте эти записи в DNS вашего домена (через панель хостинга)
6. После верификации домен будет готов
7. Обновите `MAILGUN_DOMAIN` в `config.php` на `backtobase.ca`

**Преимущества:**
- ✅ Можно отправлять на **любые** email-адреса (без ограничений)
- ✅ Клиенты НЕ должны ничего подтверждать
- ✅ Профессионально и удобно
- ✅ Лучшая доставляемость (не попадает в спам)

**После добавления домена:**
- Клиент просто создает бронирование
- Система автоматически отправляет письмо на его email
- Никаких подтверждений не требуется!

---

### Вариант 2: Использовать Sandbox для тестирования

**Для тестирования можно:**
- Добавить несколько тестовых email-адресов в список разрешенных
- Тестировать отправку писем на эти адреса
- После тестирования перейти на свой домен

---

### Вариант 3: Перейти на платный план Mailgun

**Mailgun Essentials ($35/месяц):**
- Включает верифицированный домен
- Можно отправлять на любые email-адреса
- 50,000+ писем/месяц

---

## 🎯 Рекомендация для вашего проекта

### Для продакшена:

**Добавьте свой домен `backtobase.ca` в Mailgun:**

1. Это позволит отправлять письма на **любые** email-адреса
2. Клиенты **НЕ должны** ничего подтверждать
3. Процесс будет автоматическим:
   - Клиент создает бронирование → система автоматически отправляет письмо
   - Никаких действий от клиента не требуется

### Для тестирования сейчас:

**Используйте Sandbox Domain:**
- Добавьте несколько тестовых email-адресов в список разрешенных
- Протестируйте отправку писем
- После тестирования перейдите на свой домен

---

## 📝 Пошаговая инструкция: Добавление домена

### Шаг 1: Добавить домен в Mailgun

1. В Mailgun Dashboard → **Sending** → **Domains**
2. Нажмите **Add New Domain**
3. Введите `backtobase.ca`
4. Выберите регион: **US** (рекомендую для Канады)
5. Нажмите **Add Domain**

### Шаг 2: Получить DNS записи

Mailgun покажет DNS записи, которые нужно добавить:

**Пример:**
```
TXT Record для верификации:
Name: @
Value: v=spf1 include:mailgun.org ~all

TXT Record для DKIM:
Name: k1._domainkey
Value: k=rsa; p=MIGfMA0GCSqGSIb3DQEBAQUAA4GNADCBiQKBgQC...

CNAME Record:
Name: email
Value: mailgun.org
```

### Шаг 3: Добавить DNS записи на хостинге

1. Зайдите в панель управления хостингом
2. Найдите настройки DNS для домена `backtobase.ca`
3. Добавьте записи, которые предоставил Mailgun
4. Сохраните изменения

### Шаг 4: Подождать верификации

1. Mailgun автоматически проверит DNS записи
2. Обычно это занимает 24-48 часов (но может быть быстрее)
3. После верификации статус домена изменится на **Verified**

### Шаг 5: Обновить config.php

После верификации обновите `config.php`:

```php
define('MAILGUN_DOMAIN', 'backtobase.ca'); // ✅ Вместо Sandbox Domain
```

---

## ✅ Итог

### С Sandbox Domain (текущая настройка):
- ❌ Нужно добавлять каждого клиента в список разрешенных
- ❌ Неудобно для продакшена
- ✅ Подходит только для тестирования

### С верифицированным доменом (рекомендуется):
- ✅ Можно отправлять на любые email-адреса
- ✅ Клиенты НЕ должны ничего подтверждать
- ✅ Процесс полностью автоматический
- ✅ Профессионально и удобно

---

## 🚀 Что делать сейчас?

1. **Для тестирования**: Используйте Sandbox Domain (текущая настройка)
   - Добавьте несколько тестовых email в список разрешенных
   - Протестируйте отправку писем

2. **Для продакшена**: Добавьте свой домен `backtobase.ca` в Mailgun
   - После верификации обновите `MAILGUN_DOMAIN` в `config.php`
   - Тогда система сможет отправлять письма всем клиентам автоматически

**Нужна помощь с добавлением домена?** Дайте знать! 🎯



