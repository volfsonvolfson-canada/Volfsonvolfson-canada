# 🔍 Диагностика: Письма не отправляются после обновления config.php

## ❌ Проблема

**После обновления `config.php` письма не отправляются и не отображаются в Mailgun Dashboard.**

---

## 🔍 Возможные причины

### Причина 1: config.php не обновился на хостинге

**Проблема:** Старый файл все еще используется на хостинге.

**Решение:**
1. Проверьте, что файл `config.php` на хостинге содержит новые значения
2. Используйте файл `check-mailgun-config.php` для проверки

---

### Причина 2: Бронирование не создано после обновления

**Проблема:** Бронирование было создано до обновления config.php.

**Решение:**
1. Создайте **НОВОЕ** бронирование после обновления config.php
2. Только новые бронирования будут использовать новые настройки

---

### Причина 3: Ошибки при отправке писем

**Проблема:** Письма пытаются отправиться, но возникают ошибки.

**Решение:**
1. Проверьте логи PHP на хостинге
2. Ищите записи: `Mailgun: Attempting to send...`

---

## 🔧 Пошаговая диагностика

### Шаг 1: Проверить config.php на хостинге

**Загрузите файл `check-mailgun-config.php` на хостинг:**

1. Загрузите файл `check-mailgun-config.php` на хостинг
2. Откройте в браузере: `https://new.backtobase.ca/check-mailgun-config.php`
3. Проверьте значения:
   - `MAILGUN_DOMAIN` должен быть: `new.backtobase.ca`
   - `MAILGUN_FROM_EMAIL` должен быть: `bookings@new.backtobase.ca`

**Если значения неправильные:**
- Перезагрузите `config.php` на хостинг
- Убедитесь, что файл загружен правильно

---

### Шаг 2: Создать новое бронирование

**ВАЖНО:** Создайте **НОВОЕ** бронирование ПОСЛЕ обновления config.php.

1. Откройте страницу комнаты: `https://new.backtobase.ca/room-basement.html`
2. Заполните форму бронирования
3. Укажите любой email (не нужно добавлять в Authorized Recipients!)
4. Создайте бронирование
5. Проверьте, отправляются ли письма

---

### Шаг 3: Проверить логи PHP

**После создания нового бронирования проверьте логи PHP:**

**Ищите записи:**
```
Mailgun: Attempting to send email to ... via domain new.backtobase.ca
Mailgun: URL: https://api.mailgun.net/v3/new.backtobase.ca/messages
Mailgun: HTTP Code: ...
```

**Если записей нет:**
- Функция отправки не вызывается
- Проверьте, что бронирование создается через API

**Если есть ошибки:**
- Они будут в логах
- Сообщите мне, какие ошибки

---

### Шаг 4: Проверить Mailgun Dashboard

**В Mailgun Dashboard:**

1. Откройте: https://app.mailgun.com/
2. Перейдите: **Sending** → **Logs**
3. Фильтруйте по домену: `new.backtobase.ca`
4. Проверьте, есть ли записи о отправленных письмах

**Если записей нет:**
- Письма не доходят до Mailgun
- Проверьте логи PHP на ошибки

---

## 📋 Что проверить

### 1. Проверка config.php на хостинге

**Используйте файл `check-mailgun-config.php`:**

1. Загрузите файл на хостинг
2. Откройте в браузере
3. Проверьте значения констант

---

### 2. Создание нового бронирования

**ВАЖНО:** Создайте новое бронирование ПОСЛЕ обновления config.php.

**Старые бронирования используют старые настройки!**

---

### 3. Проверка логов PHP

**После создания бронирования проверьте логи PHP:**

**Должны быть записи:**
```
Booking API: Attempting to send confirmation email to guest: ...
Mailgun: Attempting to send email to ... via domain new.backtobase.ca
Mailgun: HTTP Code: 200 (или другая ошибка)
```

---

## 🎯 Что делать сейчас

1. **Загрузите `check-mailgun-config.php` на хостинг**
2. **Откройте его в браузере** и проверьте значения
3. **Создайте НОВОЕ бронирование** после проверки config.php
4. **Проверьте логи PHP** после создания бронирования
5. **Проверьте Mailgun Dashboard** → Sending → Logs

---

## 📞 Сообщите результаты

**Проверьте и сообщите:**

1. **Что показывает `check-mailgun-config.php`?**
   - Правильные ли значения `MAILGUN_DOMAIN` и `MAILGUN_FROM_EMAIL`?

2. **Создано ли новое бронирование ПОСЛЕ обновления config.php?**
   - Старые бронирования не будут использовать новые настройки

3. **Что в логах PHP после создания бронирования?**
   - Есть ли записи `Mailgun: Attempting to send...`?
   - Есть ли ошибки?

4. **Есть ли записи в Mailgun Dashboard → Sending → Logs?**

**Сообщите результаты проверки!** 🚀



