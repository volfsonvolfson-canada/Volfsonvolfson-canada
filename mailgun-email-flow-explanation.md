# 📧 Куда отправляются письма при бронировании

## ✅ Тестовый файл vs Реальное бронирование

### Тестовый файл (`test_mailgun.php`):

**Отправляет письмо на:** `backtobasewellness@gmail.com` (email хозяина)

**Почему:** Это тестовый файл - он проверяет, что Mailgun работает. Он использует `MAILGUN_HOST_EMAIL` для теста.

---

## 📨 При реальном бронировании отправляются ДВА письма:

### Письмо 1: Гостю (клиенту)

**Отправляется на:** Email гостя (который указан при бронировании)

**Функция:** `sendBookingConfirmation($booking)`

**Код:**
```php
$email = $booking['email'] ?? ''; // Email гостя из формы бронирования
// ...
sendEmail($email, $subject, $htmlContent); // Отправка на email гостя
```

**Тема:** "Booking Confirmation - Back to Base Hotel"

**Содержит:** Код подтверждения, детали бронирования

---

### Письмо 2: Хозяину (вам)

**Отправляется на:** `backtobasewellness@gmail.com` (email хозяина)

**Функция:** `sendBookingRequestToHost($booking)`

**Код:**
```php
$hostEmail = MAILGUN_HOST_EMAIL ?? ''; // backtobasewellness@gmail.com
// ...
sendEmail($hostEmail, $subject, $htmlContent); // Отправка на email хозяина
```

**Тема:** "New Booking Request - Back to Base Hotel"

**Содержит:** Детали бронирования гостя, ссылка на админ-панель

---

## ⚠️ Важно для Sandbox Domain

### Для письма гостю:

**Email гостя (`volfsonvolfson.canada@gmail.com`) должен быть в списке авторизованных получателей!**

Если email не в списке:
- ❌ Письмо гостю НЕ отправится
- ✅ Письмо хозяину отправится (если хозяин в списке)

**Решение:**
1. Mailgun Dashboard → **Sending** → **Domain Settings**
2. Выберите Sandbox Domain
3. Вкладка **Authorized Recipients**
4. Нажмите **Add Recipient**
5. Добавьте `volfsonvolfson.canada@gmail.com`
6. Подтвердите email (Mailgun отправит письмо с подтверждением)

---

## 🔍 Проверка: Какие письма отправляются

### При создании бронирования:

1. **Письмо гостю:**
   - На: `volfsonvolfson.canada@gmail.com` (email из формы)
   - Тема: "Booking Confirmation - Back to Base Hotel"
   - Отправляется только если email в списке авторизованных

2. **Письмо хозяину:**
   - На: `backtobasewellness@gmail.com` (из `MAILGUN_HOST_EMAIL`)
   - Тема: "New Booking Request - Back to Base Hotel"
   - Отправляется если хозяин в списке авторизованных

---

## 📊 Почему тестовый файл отправляет на другую почту

**Тестовый файл `test_mailgun.php`:**

```php
$testEmail = MAILGUN_HOST_EMAIL ?: MAILGUN_FROM_EMAIL;
// Использует email хозяина для теста
```

**Это правильно для теста!** Тестовый файл проверяет, что Mailgun работает, отправляя письмо на известный email (хозяина).

**Но при реальном бронировании:**
- Письмо гостю отправляется на email из формы (`volfsonvolfson.canada@gmail.com`)
- Письмо хозяину отправляется на `backtobasewellness@gmail.com`

---

## ✅ Что нужно сделать

### Для отправки письма гостю:

1. **Добавьте email гостя в список авторизованных:**
   - Mailgun Dashboard → **Sending** → **Domain Settings**
   - Выберите Sandbox Domain
   - Вкладка **Authorized Recipients**
   - Добавьте `volfsonvolfson.canada@gmail.com`
   - Подтвердите email

2. **Создайте тестовое бронирование:**
   - Откройте страницу комнаты (например, `room-basement.html`)
   - Заполните форму с email `volfsonvolfson.canada@gmail.com`
   - Нажмите "Book"

3. **Проверьте результат:**
   - Mailgun Dashboard → **Sending** → **Logs**
   - Должно быть 2 отправки:
     - На `volfsonvolfson.canada@gmail.com` (гостю)
     - На `backtobasewellness@gmail.com` (хозяину)

---

## 🎯 Итог

**Тестовый файл:**
- ✅ Отправляет на `backtobasewellness@gmail.com` (это правильно для теста)

**Реальное бронирование:**
- ✅ Отправляет гостю на email из формы (`volfsonvolfson.canada@gmail.com`)
- ✅ Отправляет хозяину на `backtobasewellness@gmail.com`

**Важно:**
- Для Sandbox Domain email гостя должен быть в списке авторизованных
- Иначе письмо гостю не отправится

**Добавьте `volfsonvolfson.canada@gmail.com` в список авторизованных получателей в Mailgun!** 🚀



