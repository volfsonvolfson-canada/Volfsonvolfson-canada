# 🔧 Верификация домена new.backtobase.ca: Статус Unverified

## ⚠️ Текущая ситуация

**Статус домена:** `Unverified`  
**Это означает:** DNS записи не найдены или неправильные

**Временно вернулись к Sandbox Domain** для работы системы.

---

## 🔍 Проверка DNS записей

### Шаг 1: Проверьте, какие DNS записи нужны

1. В Mailgun Dashboard → **Sending** → **Domains**
2. Найдите домен `new.backtobase.ca`
3. Нажмите на него или кнопку **"Verify DNS Settings"**
4. Mailgun покажет, какие DNS записи нужны

**Обычно это:**
- TXT запись для верификации домена
- TXT запись для DKIM (подпись писем)
- CNAME запись (опционально)

---

### Шаг 2: Проверьте, какие записи вы добавили

**Ранее вы добавили:**
1. TXT запись для DKIM: `krs._domainkey.new` → (длинное значение)
2. TXT запись для SPF: `new` → `v=spf1 include:mailgun.org ~all`

**Но Mailgun может требовать дополнительные записи!**

---

### Шаг 3: Проверьте, что Mailgun требует

В Mailgun Dashboard для домена `new.backtobase.ca` должны быть показаны:

1. **TXT запись для верификации** (обычно для поддомена `new`):
   - Name: `new` (или `new.backtobase.ca`)
   - Value: Что-то вроде `v=spf1 include:mailgun.org ~all` или другое значение от Mailgun

2. **TXT запись для DKIM:**
   - Name: `krs._domainkey.new` (или `krs._domainkey.new.backtobase.ca`)
   - Value: Длинное значение от Mailgun

3. **CNAME запись (если требуется):**
   - Name: `email.new` (или другое)
   - Value: `mailgun.org`

---

## 🔧 Что нужно сделать

### Вариант 1: Проверить DNS записи

1. **В Mailgun Dashboard** для домена `new.backtobase.ca` посмотрите:
   - Какие DNS записи требуются
   - Какие записи уже найдены (зеленые галочки)
   - Какие записи не найдены (красные крестики)

2. **Проверьте на хостинге:**
   - Зайдите в DNS Zone Editor на GreenGeeks
   - Проверьте, что все записи добавлены правильно
   - Убедитесь, что имена записей точно совпадают с тем, что требует Mailgun

---

### Вариант 2: Исправить имена DNS записей

**Проблема может быть в именах записей!**

Для поддомена `new.backtobase.ca` имена записей могут быть:
- `new` (короткое имя)
- `new.backtobase.ca` (полное имя)

**Проверьте в Mailgun, какой формат имени требуется.**

---

### Вариант 3: Добавить недостающие записи

Mailgun может требовать дополнительные записи, которых нет:

1. **Проверьте в Mailgun**, какие записи не найдены
2. **Добавьте недостающие записи** на хостинге
3. **Подождите распространения DNS** (10 минут - 24 часа)

---

## 📋 Пошаговая инструкция по исправлению

### Шаг 1: Посмотрите требования Mailgun

1. Mailgun Dashboard → **Sending** → **Domains**
2. Нажмите на `new.backtobase.ca`
3. Посмотрите список DNS записей:
   - ✅ Зеленые галочки - записи найдены
   - ❌ Красные крестики - записи не найдены или неправильные

### Шаг 2: Проверьте DNS записи на хостинге

1. Зайдите в GreenGeeks → **DNS Zone Editor**
2. Убедитесь, что все записи от Mailgun добавлены
3. Проверьте, что имена записей точно совпадают

### Шаг 3: Исправьте неправильные записи

Если какие-то записи неправильные:
1. Удалите старые записи
2. Добавьте новые с правильными значениями от Mailgun
3. Сохраните изменения

### Шаг 4: Подождите распространения DNS

1. После исправления DNS подождите 10 минут - 24 часа
2. Mailgun автоматически проверит записи
3. Статус должен измениться на **Verified**

---

## 🔍 Проверка через онлайн-инструменты

### Проверьте DNS записи через mxtoolbox.com:

1. Откройте https://mxtoolbox.com
2. Введите `new.backtobase.ca`
3. Выберите **TXT Lookup**
4. Проверьте, что видны ваши TXT записи:
   - `krs._domainkey.new` → (длинное значение)
   - `new` → `v=spf1 include:mailgun.org ~all`

**Если записи не видны** - они еще не распространились или добавлены неправильно.

---

## ⚠️ Частые проблемы

### Проблема 1: Неправильное имя записи

**Для поддомена `new.backtobase.ca`:**

- Mailgun может требовать: `new` (короткое)
- Или: `new.backtobase.ca` (полное)
- Или: `krs._domainkey.new` (для DKIM)

**Решение:** Проверьте в Mailgun, какой формат имени требуется, и используйте точно такой же.

---

### Проблема 2: DNS записи еще не распространились

**После добавления DNS записей:**
- Распространение может занять 10 минут - 48 часов
- Обычно 1-4 часа

**Решение:** Подождите и периодически проверяйте статус в Mailgun.

---

### Проблема 3: Не все записи добавлены

**Mailgun требует несколько записей:**
- TXT для верификации
- TXT для DKIM
- Возможно CNAME

**Решение:** Проверьте в Mailgun, какие записи не найдены, и добавьте их.

---

## ✅ Что делать сейчас

### Временно (для работы системы):

1. ✅ **Используем Sandbox Domain** (уже обновлено в `config.php`)
2. ✅ Система работает с Sandbox Domain
3. ✅ Можно отправлять на авторизованные email-адреса

---

### Для верификации домена:

1. **Проверьте в Mailgun Dashboard:**
   - Какие DNS записи требуются
   - Какие записи не найдены (красные крестики)

2. **Проверьте на хостинге:**
   - Все ли записи добавлены
   - Правильные ли имена записей

3. **Исправьте проблемы:**
   - Добавьте недостающие записи
   - Исправьте неправильные имена
   - Подождите распространения DNS

4. **После верификации:**
   - Обновите `MAILGUN_DOMAIN` на `new.backtobase.ca`
   - Обновите `MAILGUN_FROM_EMAIL` на `bookings@new.backtobase.ca`
   - Загрузите обновленный `config.php` на хостинг

---

## 📊 Текущая конфигурация

**Временно используем Sandbox Domain:**
```php
define('MAILGUN_DOMAIN', 'sandbox2856f0aed70c40a8b13e5a5f4f52c3bc.mailgun.org');
define('MAILGUN_FROM_EMAIL', 'backtobasewellness@gmail.com');
```

**После верификации домена:**
```php
define('MAILGUN_DOMAIN', 'new.backtobase.ca');
define('MAILGUN_FROM_EMAIL', 'bookings@new.backtobase.ca');
```

---

## 🎯 Итог

**Текущая ситуация:**
- ⚠️ Домен `new.backtobase.ca` имеет статус `Unverified`
- ✅ Временно используем Sandbox Domain для работы
- ⏳ Нужно завершить верификацию домена

**Что делать:**
1. Проверьте в Mailgun, какие DNS записи не найдены
2. Исправьте/добавьте записи на хостинге
3. Подождите верификации
4. После верификации переключитесь на верифицированный домен

**Система работает с Sandbox Domain, но для лучшей доставляемости нужно завершить верификацию домена!** 🚀



