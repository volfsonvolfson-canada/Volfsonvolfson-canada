# 🔍 Диагностика: Почему письма не отображаются в Mailgun Dashboard

## ❌ Проблема

Письма не отображаются в Mailgun Dashboard → Sending → Logs

**Это означает:** Письма вообще НЕ отправляются через Mailgun API

---

## 🔍 Возможные причины

### 1. Бронирование не создается через API

**Проблема:** Если вы создаете бронирование через `order.html` (localStorage), то API `create_booking` НЕ вызывается, и письма НЕ отправляются.

**Решение:** Создавайте бронирование через форму на странице комнаты (например, `room-basement.html`), а не через `order.html`.

---

### 2. Ошибка при отправке (но не видна)

**Проблема:** Ошибка при отправке может логироваться, но не отображаться пользователю.

**Решение:** Проверьте логи PHP на хостинге (добавил подробное логирование в `email_service.php`).

---

### 3. Неправильный домен или API ключ

**Проблема:** Mailgun API отклоняет запрос из-за неправильного домена или ключа.

**Решение:** Проверьте настройки в `config.php`.

---

## 🔧 Что делать для диагностики

### Шаг 1: Загрузите отладочный файл

1. Загрузите файл `debug_email_sending.php` на хостинг
2. Откройте в браузере: `https://new.backtobase.ca/debug_email_sending.php`
3. Проверьте результат:
   - Правильно ли настроены ключи
   - Работает ли отправка тестового письма
   - Есть ли последние бронирования в базе

---

### Шаг 2: Проверьте логи PHP на хостинге

После создания бронирования проверьте логи PHP на хостинге. Там должны быть записи:

```
Mailgun: Attempting to send email to ...
Mailgun: URL: ...
Mailgun: HTTP Code: ...
Mailgun: Response: ...
```

**Если этих записей нет** - функция отправки не вызывается.  
**Если есть ошибки** - они будут в логах.

---

### Шаг 3: Проверьте, создается ли бронирование в базе

1. Проверьте базу данных:
   - Таблица `bookings` - должна быть новая запись
   - Если записи нет - бронирование не создается через API

2. Используйте `debug_email_sending.php` - он покажет последние бронирования

---

### Шаг 4: Создайте бронирование правильно

**Важно:** Бронирование должно создаваться через форму на странице комнаты!

1. Откройте страницу комнаты: `https://new.backtobase.ca/room-basement.html`
2. Заполните форму бронирования
3. Нажмите "Book" или "Submit"
4. Должен произойти редирект на `booking-confirmation.html`

**НЕ создавайте бронирование через `order.html`** - там данные только сохраняются в localStorage!

---

## 📊 Проверка через debug_email_sending.php

Отладочный файл покажет:

1. **Настройки Mailgun:**
   - Правильно ли настроены ключи
   - Какой домен используется

2. **Тест отправки:**
   - Попытка отправить тестовое письмо
   - Результат отправки
   - Ошибки, если есть

3. **Последние бронирования:**
   - Есть ли бронирования в базе
   - Когда они созданы

4. **Логи активности:**
   - Записи о попытках отправки писем

---

## ⚠️ Частые ошибки

### Ошибка 1: Бронирование создается через order.html

**Симптом:** Письма не отправляются, в Mailgun нет записей

**Решение:** Создавайте бронирование через форму на странице комнаты

---

### Ошибка 2: Ошибка 401 Unauthorized

**Причина:** Неправильный API ключ

**Решение:** Проверьте `MAILGUN_API_KEY` в `config.php`

---

### Ошибка 3: Ошибка 404 Not Found

**Причина:** Неправильный домен

**Решение:** Проверьте `MAILGUN_DOMAIN` в `config.php`

---

### Ошибка 4: Ошибка "Sender address not verified"

**Причина:** Email отправителя не в списке авторизованных (для Sandbox)

**Решение:** Добавьте email в список авторизованных получателей

---

## ✅ Что делать сейчас

1. **Загрузите `debug_email_sending.php` на хостинг**
2. **Откройте его в браузере**
3. **Проверьте результат:**
   - Работает ли тестовая отправка?
   - Есть ли ошибки?
   - Есть ли бронирования в базе?

4. **Создайте бронирование правильно:**
   - Через форму на странице комнаты
   - НЕ через order.html

5. **Проверьте логи PHP на хостинге** после создания бронирования

---

## 📝 Добавлено логирование

Добавлено подробное логирование в `email_service.php`:

- Логирование попытки отправки
- Логирование URL запроса
- Логирование HTTP кода ответа
- Логирование ответа от Mailgun
- Логирование ошибок

**Теперь в логах PHP на хостинге будут видны все попытки отправки писем!**

---

**Загрузите `debug_email_sending.php` на хостинг и проверьте результат. Сообщите, что показывает файл!** 🚀



