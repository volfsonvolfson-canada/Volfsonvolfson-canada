# ✅ Переключение на верифицированный домен new.backtobase.ca

## 🔄 Что изменилось

**Обновлен `config.php`:**

- ✅ **MAILGUN_DOMAIN** изменен с Sandbox Domain на `new.backtobase.ca`
- ✅ **MAILGUN_FROM_EMAIL** изменен на `bookings@new.backtobase.ca` (email на вашем домене)

---

## ✅ Преимущества верифицированного домена

1. **Можно отправлять на любые email-адреса** (без ограничений Sandbox)
2. **Лучшая доставляемость** (не попадает в спам)
3. **Профессионально** - письма от `@new.backtobase.ca`
4. **Клиенты НЕ должны подтверждать email**
5. **Нет ограничений по получателям**

---

## ⚠️ Важно: Проверьте статус домена

### Перед использованием убедитесь:

1. В Mailgun Dashboard → **Sending** → **Domains**
2. Найдите домен `new.backtobase.ca`
3. Проверьте статус:
   - ✅ **Verified** - домен верифицирован, можно использовать!
   - ⏳ **Pending** - еще не верифицирован (подождите)
   - ⚠️ **Unverified** - DNS записи не найдены или неправильные

**Если статус Verified** - все готово! Загрузите обновленный `config.php` на хостинг.

**Если статус Pending или Unverified** - нужно завершить верификацию домена.

---

## 📝 Что нужно сделать

### 1. Проверьте статус домена в Mailgun

- Mailgun Dashboard → **Sending** → **Domains**
- Найдите `new.backtobase.ca`
- Статус должен быть **Verified**

### 2. Загрузите обновленный `config.php` на хостинг

Файл уже обновлен с новым доменом.

### 3. Протестируйте отправку писем

1. Создайте тестовое бронирование через форму на странице комнаты
2. Проверьте Mailgun Dashboard → **Sending** → **Logs**
3. Проверьте почтовые ящики (и папку Spam)

---

## 🔍 Как проверить, что используется правильный домен

### В Mailgun Dashboard → Sending → Logs:

1. Откройте детали отправки
2. Посмотрите поле **"From"** или **"Domain"**
3. Должно быть: `bookings@new.backtobase.ca` или домен `new.backtobase.ca`

---

## ⚠️ Если домен еще не верифицирован

Если статус домена `new.backtobase.ca` в Mailgun **не Verified**:

1. **Вернитесь к Sandbox Domain временно:**
   ```php
   define('MAILGUN_DOMAIN', 'sandbox2856f0aed70c40a8b13e5a5f4f52c3bc.mailgun.org');
   define('MAILGUN_FROM_EMAIL', 'backtobasewellness@gmail.com');
   ```

2. **Завершите верификацию домена:**
   - Проверьте DNS записи в Mailgun
   - Убедитесь, что они добавлены на хостинге
   - Подождите верификации (24-48 часов)

3. **После верификации** переключитесь на `new.backtobase.ca`

---

## ✅ Итог

**Обновлено:**
- ✅ `MAILGUN_DOMAIN` = `new.backtobase.ca` (верифицированный домен)
- ✅ `MAILGUN_FROM_EMAIL` = `bookings@new.backtobase.ca`

**Что делать:**
1. Проверьте статус домена `new.backtobase.ca` в Mailgun (должен быть Verified)
2. Загрузите обновленный `config.php` на хостинг
3. Протестируйте отправку писем

**После переключения письма будут доставляться лучше!** 🚀

---

## 📊 Проверка после переключения

После загрузки обновленного `config.php`:

1. Создайте тестовое бронирование
2. Проверьте Mailgun Dashboard → **Sending** → **Logs**
3. В деталях отправки должно быть:
   - **From:** `bookings@new.backtobase.ca` или `Back to Base Hotel <bookings@new.backtobase.ca>`
   - **Domain:** `new.backtobase.ca`

**Если все правильно - письма будут доставляться лучше!** ✅



