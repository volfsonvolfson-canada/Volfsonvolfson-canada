# 🔍 Проверка существующих DNS записей Mailgun

## 📋 Что нужно проверить в списке записей

**Прокрутите вниз и проверьте, есть ли уже записи Mailgun:**

---

## ✅ Записи, которые нужно найти:

### 1. TXT записи (DKIM, SPF, DMARC)

**Ищите записи типа TXT с именами:**
- `krs._domainkey.new.backtobase.ca.` (или `krs._domainkey`)
- `new.backtobase.ca.` с SPF записью (`v=spf1 include:mailgun.org ~all`)
- `_dmarc.new.backtobase.ca.` (или `_dmarc`)

**Если есть кнопка "TXT" в фильтре - нажмите на нее, чтобы увидеть только TXT записи.**

---

### 2. MX записи для Mailgun

**Ищите MX записи с:**
- `Priority: 10` и `Destination: mxa.mailgun.org`
- `Priority: 10` и `Destination: mxb.mailgun.org`

**Сейчас в списке есть MX запись:**
- `Priority: 0`, `Destination: new.backtobase.ca` - это НЕ Mailgun запись

---

### 3. CNAME запись для email

**Ищите CNAME запись:**
- `Name: email.new.backtobase.ca.` (или `email`)
- `Record: mailgun.org`

**Сейчас в списке есть:**
- `mail.new.backtobase.ca.` → `new.backtobase.ca` - это НЕ Mailgun запись

---

## 🎯 Что делать дальше

### Вариант 1: Записи Mailgun уже есть

**Если вы видите записи Mailgun в списке:**
- Проверьте, что они правильные (сравните значения с инструкцией)
- Если все правильно - переходите к активации в Mailgun

---

### Вариант 2: Записи Mailgun отсутствуют

**Если записей Mailgun нет в списке:**

1. **Нажмите кнопку "Add Record"** (с выпадающим списком)
2. **Выберите тип записи** (TXT, MX, CNAME)
3. **Добавьте каждую запись** по инструкции ниже

---

## 📝 Инструкция по добавлению записей

### Если записей Mailgun нет, нажмите "Add Record" и добавьте:

---

### Запись 1: TXT (DKIM - krs._domainkey)

**После нажатия "Add Record" → выберите "TXT":**

1. **Name:** `krs._domainkey` (без `.new.backtobase.ca`!)
2. **TTL:** `3600`
3. **TXT Data:** Вставьте весь ключ:
   ```
   k=rsa; p=
   ```
4. **Сохраните**

---

### Запись 2: TXT (SPF)

**Нажмите "Add Record" → "TXT":**

1. **Name:** Оставьте **пустым** или введите `@` (для корневого домена)
2. **TTL:** `3600`
3. **TXT Data:** `v=spf1 include:mailgun.org ~all`
4. **Сохраните**

---

### Запись 3: MX (mxa.mailgun.org)

**Нажмите "Add Record" → "MX":**

1. **Name:** Оставьте **пустым** или введите `@`
2. **TTL:** `3600`
3. **Priority:** `10`
4. **Destination:** `mxa.mailgun.org`
5. **Сохраните**

---

### Запись 4: MX (mxb.mailgun.org)

**Нажмите "Add Record" → "MX":**

1. **Name:** Оставьте **пустым** или введите `@`
2. **TTL:** `3600`
3. **Priority:** `10`
4. **Destination:** `mxb.mailgun.org`
5. **Сохраните**

---

### Запись 5: CNAME (email)

**Нажмите "Add Record" → "CNAME":**

1. **Name:** `email` (без домена!)
2. **TTL:** `3600`
3. **Record:** `mailgun.org`
4. **Сохраните**

---

### Запись 6: TXT (DMARC)

**Нажмите "Add Record" → "TXT":**

1. **Name:** `_dmarc` (без домена!)
2. **TTL:** `3600`
3. **TXT Data:** `v=DMARC1; p=none; pct=100; fo=1; ri=3600; rua=mailto:ca560f54@dmarc.mailgun.org,mailto:6025edc5@inbox.ondmarc.com; ruf=mailto:ca560f54@dmarc.mailgun.org,mailto:6025edc5@inbox.ondmarc.com;`
4. **Сохраните**

---

## 🔍 Как быстро проверить

**В интерфейсе есть фильтры по типу:**

1. **Нажмите кнопку "TXT"** в фильтрах (над таблицей)
2. Это покажет только TXT записи
3. Проверьте, есть ли там записи Mailgun

**Аналогично для MX и CNAME записей.**

---

## 📞 Что мне нужно от вас

**Проверьте список и сообщите:**

1. **Есть ли TXT записи** с `krs._domainkey`, SPF, или `_dmarc`?
2. **Есть ли MX записи** с `mxa.mailgun.org` или `mxb.mailgun.org`?
3. **Есть ли CNAME запись** с именем `email` и значением `mailgun.org`?

**Если записей нет - добавьте их через кнопку "Add Record"!** 🚀



